#
# widget layout configuration, avalaible widgets:
#   see widgets folder, every file is a widget
# for widget configuration, edit widget file
#
# col=2
sysinfo   network
disks     partitions
services  apt
os        lastexec
lastexec
